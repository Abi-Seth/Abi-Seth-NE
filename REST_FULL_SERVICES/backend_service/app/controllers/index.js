import { createUser, fetchUsers, getCurrentUser, login } from './user.controller'
import { createEmployeeRecord, fetchEmployeeRecords } from './employee.controller'

export {
    createUser,
    fetchUsers,
    getCurrentUser,
    login,
    createEmployeeRecord,
    fetchEmployeeRecords,
}